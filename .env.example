# Public HTTPS URL where this server will be reachable, no trailing slash.
# e.g. https://acc-mcp.onrender.com
PUBLIC_BASE_URL=

# From your APS app (https://aps.autodesk.com/myapps) — must be a
# "Traditional Web App" (3-legged) type, NOT Server-to-Server.
APS_CLIENT_ID=uSm0dhmIMVnaJziTqcI9IQYvAGpClnTW
APS_CLIENT_SECRET=1VzAYIHf3qMv5Lmx

# Any long random string — used to sign the MCP session tokens.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

PORT=3000
